
# For better performance, replace ``-O'' with whatever
# the best optimization flag is for your computer.
# For Sun's compilers under Solaris, ``-fast'' works well.
# For gcc, ``-O2'' works well.

sim4-mod:
	gcc -o sim4-mod -I. -O *.c -lm
clean:
	rm -f sim4-mod *.o
